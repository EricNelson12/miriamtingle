# Cloudflare R2 config
# Copy this file to .env and fill in the values.
# NEVER commit .env to git.

# The public base URL your R2 bucket is served from
# (e.g. a custom domain or the R2.dev public bucket URL).
# Images are requested as `${R2_PUBLIC_URL}/<name>.webp`.
R2_PUBLIC_URL=
